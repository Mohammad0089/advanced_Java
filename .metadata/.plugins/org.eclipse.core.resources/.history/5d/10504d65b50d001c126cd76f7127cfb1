package functional_Programming_with_Java;
import java.util.function.*;
//import java.util.function.BiFunction;


public class test{

	protected static class Person{
		String name;
		Integer age;
		Person(String n, Integer age){
			this.name=n;
			this.age=age;			
		}
	}
	
	protected static class DataLoader{
		
		NoArgFunction<Person> loader;
		DataLoader(Boolean t){
		if(t) {
			loader = this::loadFake;
		}
		else {
			loader= this::loadReal;
		}
			//t ? loader = this::loadFake : loader = this::loadReal;

		}
		
		private Person loadReal() {
			return new Person("Real",35);
		}
		private Person loadFake() {
			return new Person("Fake",100);
		}
	}
	
	public static void main(String []a) {
		
		DataLoader m1 = new DataLoader(true);
		Person m = m1.loader.apply();
		System.out.println(m.name + " "+ m.age);
		
	}
}